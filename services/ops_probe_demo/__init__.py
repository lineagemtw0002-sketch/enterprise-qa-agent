"""运维探针演示件（BYOC 连接器的客户环境那一端）。

⚠️ **这不是产品交付件。** `docs/aiops_module_design.md` 明确写着"真实的 BYOC
连接器进程（客户环境里响应 query_request/exec_request 帧的那一端）不在本项目
范围内，本阶段只做平台侧协议"。这个包的存在只有一个目的：**给平台侧那套协议
做端到端验证，并让运维塔台大屏上有真实数据可看**。

定位跟 `services/tenant_kb_demo/` 一样——模拟客户那一侧的系统，不打包、不分发、
不做断线重连/凭证轮转策略这些真实部署才需要的东西。
"""
