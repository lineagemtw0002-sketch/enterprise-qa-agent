# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# QA Test credentials and config backups
config/test_credentials.yaml
config/settings.yaml.qa_backup
config/settings.yaml.bak

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject
.settings/
*.sublime-project
*.sublime-workspace

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# Project-specific
data/
cache/
logs/
*.log
traces.jsonl

# router LoRA (Phase 3, docs/chitchat_intent_design.md)：训练产物本身
# （router_lora_data/adapters_v1/adapters.safetensors 等，~21MB）要提交，
# 但这两样是构建管线里的中间大文件、完全可以用已提交的脚本 + adapter 重新
# 生成，不值得进 git 历史（fused_hf_v1 反量化后 ~2.9GB，GGUF ~1.5GB）：
router_lora_data/fused_hf_v1/
router_lora_data/*.gguf
# 训练过程中的中间 checkpoint（0000136_adapters.safetensors 等），只保留
# 最终的 adapters.safetensors + adapter_config.json：
router_lora_data/adapters_v1/0*_adapters.safetensors

# Database / SQLite
checkpoints.sqlite*
*.db
*.db-journal

# Playwright MCP cache
.playwright-mcp/

# Node modules
frontend/node_modules/
#
# frontend/package-lock.json 刻意**不**忽略。它此前跟 node_modules/ 一起被
# 归在这条注释下忽略掉，但两者性质相反：node_modules/ 是可从 lockfile 重建的
# 产物，lockfile 本身才是那份"该装哪些确切版本"的事实来源，丢掉它 package.json
# 里的 ^5.3.0 这类范围就会让不同时间的 npm install 装出不同版本组合。
# 2026-08-27 已真实付过一次代价：运维塔台新增的两个 @fontsource 字体包只进了
# package.json，别的 checkout 装不到，vite 解析不到依赖直接白屏。
# 跟 Python 侧 requirements.lock 是同一层保障（见 Dockerfile / ci.yml 注释）。

# MCP sync artifacts
.mcp_dirty_snap/
.mcp_sync_export/

# Temporary test scripts and files
tmp_test_*.py
.tmp_sync_manifest.tsv
picture/

# Auto-coder skill cache
.github/skills/auto-coder/.spec_hash

# Secrets
.env.local
.env.*.local
secrets.yaml
nonexistent_traces.jsonl/

# Evaluation reports
reports/
benchmark_report_*.json
src/参考.html

# macOS
.DS_Store

# LibreOffice 锁文件
.~lock.*#
