# Modular RAG MCP Server - Configuration
# This is the main configuration file for the MCP Server.
# Edit this file to configure LLM, Embedding, VectorStore, and other services.

# =============================================================================
# LLM Configuration
# =============================================================================
llm:
  provider: "ollama"
  model: "qwen2.5:7b"
  base_url: "http://localhost:11434"
  temperature: 0.0
  max_tokens: 4096

# =============================================================================
# Embedding Configuration
# =============================================================================
embedding:
  provider: "ollama"
  model: "nomic-embed-text"
  base_url: "http://localhost:11434"
  dimensions: 768

# =============================================================================
# Vision LLM Configuration (for Image Captioning)
# =============================================================================
vision_llm:
  enabled: true
  provider: "openai"  # no OllamaVisionLLM exists; reuse OpenAIVisionLLM against Ollama's OpenAI-compatible endpoint
  model: "qwen2.5vl:7b"
  base_url: "http://localhost:11434/v1"
  api_key: "ollama"  # unchecked by Ollama, but OpenAIVisionLLM requires a non-empty value
  max_image_size: 2048  # Max dimension (width/height) for image compression

# =============================================================================
# Vector Store Configuration
# =============================================================================
vector_store:
  provider: "chroma"  # Options: chroma, qdrant, pinecone
  persist_directory: "./data/db/chroma"
  collection_name: "knowledge_hub"

# =============================================================================
# Retrieval Configuration
# =============================================================================
retrieval:
  dense_top_k: 20
  sparse_top_k: 20
  fusion_top_k: 10
  rrf_k: 60  # RRF constant

# =============================================================================
# Rerank Configuration
# =============================================================================
rerank:
  enabled: true
  provider: "cross_encoder"  # Options: none, cross_encoder, dashscope, llm
  model: "BAAI/bge-reranker-base"  # local, no API key, supports zh/en
  top_k: 5

  # 进 cross-encoder 的候选池上限（只作用于多库并行召回，见
  # src/core/query_engine/rerank_pool.py）。上限 =
  #   max(pool_per_collection × 候选库数, pool_min, top_k × 2)
  # 6 库 = 30 条，12 库 = 60 条。**必须随库数增长**，写成常数会退化成
  # "全局预算饿死库"——实测截到 20 条时只剩 4~5 个库有代表，召回从 25/30 掉到 19/30。
  # 实测（30 条正向问题、6 库）：不截 60 条 = 25/30 召回 / 1669ms；
  # 截到 30 条 = 25/30 召回 / ~700ms，关键事实还从 29/30 涨到 30/30
  # （截掉低分噪音后不再有弱候选被 cross-encoder 抬上来挤掉正确答案）。
  # pool_per_collection 设为 0 关闭截断，退回"全部候选都进重排"。
  pool_per_collection: 5
  pool_min: 20

# =============================================================================
# Evaluation Configuration
# =============================================================================
evaluation:
  enabled: true
  provider: "ragas"  # Options: ragas, deepeval, custom
  metrics:
    - "hit_rate"
    - "mrr"
    - "faithfulness"

# =============================================================================
# Observability Configuration
# =============================================================================
observability:
  log_level: "INFO"  # Options: DEBUG, INFO, WARNING, ERROR
  trace_enabled: true
  trace_file: "./logs/traces.jsonl"
  structured_logging: true

# =============================================================================
# Ingestion Configuration
# =============================================================================
ingestion:
  chunk_size: 1000
  chunk_overlap: 200
  splitter: "recursive"  # Options: recursive, semantic, fixed_length
  batch_size: 100
  max_workers: 3  # Transform 阶段 LLM 调用的最大并发数
  
  # Chunk Refiner Configuration (C5)
  chunk_refiner:
    use_llm: false  # Set to true to enable LLM-based refinement (requires LLM config)
    # When use_llm is false or LLM call fails, falls back to rule-based refinement

  # Metadata Enricher Configuration (C6)
  metadata_enricher:
    use_llm: false  # Set to true to enable LLM-based enrichment (requires LLM config)
    # When use_llm is false or LLM call fails, falls back to rule-based enrichment

  # Document Summary Configuration — 层次化检索索引的文档摘要层
  # (src/ingestion/hierarchy/doc_summary.py)
  doc_summary:
    use_llm: false  # Set to true to enable LLM-based summarization (requires LLM config)
    # When use_llm is false or LLM call fails, falls back to rule-based summary

    # 检索时的文档级粗筛（query_knowledge_hub.py::_narrow_by_document_summary）。
    # 预算分配与置信门控的逻辑在 src/core/query_engine/narrow_plan.py，
    # 完整设计与实测见 docs/hierarchical_narrowing_redesign.md。
    narrow:
      # ⚠️ 默认关闭 = 平铺检索（层次化上线前的行为）。
      # 理由不是"层次化检索设计错了"，而是**它的前提在当前全部语料上从未成立**：
      # 实测 32 个非空 collection 里业务库的 块/文档 比全是 1.00（含 mmarco、
      # product_req_kb），而 use_llm: false 时摘要就是"标题 + 正文前 600 字"，
      # 摘要向量≈正文向量——这一层等于拿一份更差的正文副本给检索器当硬门禁。
      # 同一批 30 条正向问题：开着 5/30，关掉 25/30。
      # 重新评估的触发条件（不是到某个日期，是到某个数据形态）：
      #   任一 collection 的 块/文档 比 > 5 且文档数 > 1000，
      #   或 use_llm 改为 true / 换成中文更强的 embedding。
      #   届时重跑 scripts/probe_summary_narrowing.py，
      #   达标线：每库预算 ≤20% 时金标文档的库内覆盖率 ≥90%。
      enabled: false
      ratio: 0.4        # 每库预算 = clamp(ceil(ratio × 库内文档数), min_docs, max_docs)
      min_docs: 20
      max_docs: 100
      min_top_score: 0.85  # 某库摘要 top1 低于它 → 该库整库参检（绝不是跳过该库）

# =============================================================================
# MCP Client Configuration (External Tool Servers)
# =============================================================================
mcp_servers:
  # 简易 MCP Server（纯 Python 内置工具）
  simple:
    transport: stdio
    command: "python"
    args: ["scripts/simple_mcp_server.py", "--stdio"]
    timeout_seconds: 30

  # Example: Tavily Web Search MCP Server
  # tavily:
  #   transport: sse
  #   url: "http://localhost:3000/sse"
  #   timeout_seconds: 30
  
  # Example: Filesystem MCP Server
  # filesystem:
  #   transport: stdio
  #   command: "npx"
  #   args: ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
  #   timeout_seconds: 30
