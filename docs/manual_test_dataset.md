# 人工测试数据集（Acme / Globex 多租户演示）

> **状态：🟢 可用（数据已摄入并实测验证，见 §0.3）　日期：2026-08-26**
>
> 实测：正向问题指定库命中 **67/70**，越权与跨企业拒绝用例 **11/11**。
> ⚠️ 但**不点名知识库直接问**时只有 29/70——原因是一个已定位、未修复的检索粗筛瓶颈，
> **演示前务必先读 §0.2**。
>
> 本文由 `scripts/generate_demo_kb_dataset.py --stage doc` **自动生成**，不要手改——
> 改语料或问题请改 `scripts/demo_kb_content/`，然后重跑脚本。
>
> 用途：拿着这份文档就能登录系统做多租户问答演示 / 人工回归。
> 每个问题都标了「预期关键事实」和「数据出处（文档编号）」，方便当场判对错。

## 0. 这份数据集长什么样

| 项 | 值 |
|---|---|
| 企业 | 2 家：Acme 有限公司（云软件 SaaS）、Globex 环球集团（跨境物流） |
| 知识库 | 每家 6 个部门库，共 12 个 |
| 本次新增语料 | 每库 101 篇，共 1212 篇（各库原有 20 篇保留，现为每库 121 篇） |
| 演示账号 | 4 个：每家企业 1 个企业管理员 + 1 个受限员工 |
| 正向问题 | 每个知识库 5 条，共 60 条 |
| 应被拒问题 | 11 条（每个账号至少 2 条） |

**两家企业的同名部门刻意用了不同的锚点事实**，所以跨企业隔离一旦失效，
演示时一眼就能看出来。最典型的一组：

| 事实 | Acme | Globex |
|---|---|---|
| 年假天数 | **15 天** | **20 天** |
| 年假顺延截止 | 次年 **3 月 31 日** | 次年 **6 月 30 日** |
| 报销需 CFO 审批的门槛 | **20000 元** | **30000 元** |
| 销售代表折扣上限 | **10%** | **8%** |
| 密码强制更换周期 | 域账号 **90 天** | WMS **60 天** |

### ⚠️ §0.1 一个必须先知道的既有问题：原有 20 篇语料跟统一口径互相矛盾

12 个库里**原本就有的那 20 篇**是 `scripts/generate_tenant_kb_corpus.py` 用随机数生成的，
同一个库里会同时存在互相打架的说法。2026-08-26 实测确认的几例：

| 库 | 原有语料里的冲突说法 |
|---|---|
| `acme_hr_admin_kb` | 「平台研发部年假 **20** 天」「客户成功部年假 **15** 天」「数据研发部年假 **15** 天」 |
| `acme_it_support_kb` | 域账号密码 **60 / 90 / 120 / 150** 天四种说法并存 |
| `acme_sales_marketing_kb` | 销售折扣上限 **10% / 15%** 并存 |
| `globex_sales_marketing_kb` | 销售折扣上限 **5% / 8% / 12%** 并存 |
| `globex_rd_product_kb` | 算法灰度门槛「下降至少 **1% / 3% / 5%**」并存 |

**本次没有删除那 20 篇**——删除要走 `BM25Indexer.remove_document`，
而它正是 CLAUDE.md §4 第 7 条那个已确认失效的 P0，不该夹带在造数据里顺手改。

采取的办法是：每个库新增一篇 **`XXX-000 现行标准速查表（统一口径）`**，
用企业里真实会有的方式写明「本表自 2026-01-01 起施行，此前按部门／枢纽分别发布的口径
一律停止适用」。演示时如果模型引用了老文档的部门特例，请翻到 `-000` 速查表核对——
**这不是模型答错，是库里确实还留着旧口径**。

### 🔴 §0.2 演示前必读：企业管理员账号目前会「查不到明明存在的内容」

**现象**：用 `alice_acme`（企业管理员，6 个库）问「域账号密码多久强制更换一次？」，
返回「未找到相关结果」，而这句话在 `ACME-IT-001` 正文里写得清清楚楚；
换成只有 1 个库的 `bob_acme` 问它权限内的问题，则一切正常。

**根因**（2026-08-26 实测定位，与本批数据无关，是既有实现的伸缩性问题）：
`query_knowledge_hub.py::_narrow_by_document_summary` 是层次化检索的粗筛层，
它把**全部候选知识库**的文档摘要命中合并，然后**按分数取全局前
`top_docs` 篇文档**（`config/settings.yaml` 里 `ingestion.doc_summary.top_docs`
默认 **5**），后续分块检索**只在这 5 篇里做**。

候选池原来是 6 库 × 20 篇 = 120 篇，取前 5 还能碰上；本批把每库补到 121 篇之后
候选池变成约 726 篇，正确文档挤不进这全局 5 篇，整条链路就返回「未找到」。

**同一份数据、同一批问题的 A/B 实测**（只改 `top_docs`，其余不变）：

| `top_docs` | 通过 |
|---|---|
| 5（当前默认） | **2 / 10** |
| 30 | **7 / 10** |

**本次没有改这个配置** —— `config/settings.yaml` 是所有会话共用的，
调它会改变全平台检索行为（也会拉高延迟），属于要单独评审的改动，不该夹带在造数据里。

⚠️ **单库账号只是受影响更小，不是免疫**：候选池是「全局前 5 篇」，
即使只有 1 个库，库内 121 篇里也可能选不中正确的那篇。实测 `bob_acme`
的 5 条正向问题：指定库 5/5，全库并行 3/5。所以「换个单库账号就没事了」是错的。

**演示时怎么办**（按可靠性排序）：
1. **在提问里点名知识库**（例如「在 IT 支持知识库里，域账号密码多久换一次」），
   这是最稳的——等价于 verify 里的 scoped 路径，实测通过率见 §0.3；
2. 优先用 `bob_acme` / `dave_globex` 这两个**单库账号**，候选池小、命中率高一些，
   但仍会漏（见上）；
3. 临时把 `ingestion.doc_summary.top_docs` 调到 30 再演示——**记得改回来**。


### §0.3 验收结果（实测，不是估计）

数据来源：`scripts/demo_dataset_results/verify_20260826_193907.json`，用真实账号走真实 ACL 跑出来的。

| 账号 | 正向问题 | 指定库命中 | 全库并行命中（真实演示路径） |
|---|---|---|---|
| `alice_acme` | 30 | **30/30** | 13/30 |
| `bob_acme` | 5 | **5/5** | 3/5 |
| `carol_globex` | 30 | **27/30** | 9/30 |
| `dave_globex` | 5 | **5/5** | 4/5 |
| **合计** | 70 | **67/70** | 29/70 |

- **「指定库命中」才是这份数据集的验收判据**——它回答「语料在不在、查不查得到」。
- 「全库并行命中」低是 §0.2 那个 `top_docs=5` 粗筛瓶颈造成的，不是数据缺陷；两列的差值 38 条就是被粗筛吃掉的量。

拒绝类问题：**11/11 通过**（判据是「检索片段里不含另一家企业的标记词」，不是「回答为空」——回答开头会回显用户问题，按整段回答做子串匹配会误判）。

#### 已知会答不准的问题（指定库也没命中）

**刻意没有为了让它们变绿去改问法或补语料**——事实本身在库里，没命中是因为同一个库里有语义更近的另一篇文档把它挤掉了。演示时避开这几条，或直接翻文档原文核对。

| 账号 | 知识库 | 问题 | 应该出自 |
|---|---|---|---|
| `carol_globex` | `globex_finance_kb` | 报销单笔超过多少金额需要 CFO 审批？ | `GLBX-FIN-002` |
| `carol_globex` | `globex_sales_marketing_kb` | 年运费承诺超过多少的大客户合同需要总部特批？ | `GLBX-SM-001` |
| `carol_globex` | `globex_customer_success_kb` | 货损理赔要在多少个工作日内完成？ | `GLBX-CS-002` |

---

## 1. 怎么用这份文档

1. 用下面任意一个账号登录（前端默认 `http://localhost:5173`，后端 `http://localhost:8000`）。
2. 在对话框里直接问「该账号可访问知识库」小节里的问题。
3. 对着「预期关键事实」判断答对没有；答错时按「数据出处」的文档编号去知识库管理页找原文核对。
4. **重点演示 🚫 小节**：那些问题应该拿到「无权访问」或「没有查到相关内容」，
   如果居然答出来了，就是权限或隔离出了问题。

⚠️ 一个容易误判的点：受限员工被拒时，系统返回的是「无权访问 / 没有权限访问这个知识库集合」；
跨企业的问题则通常表现为「没有查到相关内容」——**两者都算通过**，
因为候选库在 ACL 收敛阶段就被 `_org_owned_collections(本企业)` 挡掉了，压根没进检索。

---

## 2. 账号与问题

### alice_acme

| 项 | 值 |
|---|---|
| 用户名 | `alice_acme` |
| 密码 | `alice_acme123` |
| 所属企业 | Acme 有限公司 |
| 角色 | 企业管理员（org_admin）（系统角色） |
| 可访问知识库 | 6 个 |

> 企业管理员，不需要显式配置知识库分组也能访问本企业全部知识库（role_store 里的隐式通配）

可访问的知识库：

- `acme_hr_admin_kb` —— Acme 有限公司 人力行政知识库
- `acme_finance_kb` —— Acme 有限公司 财务知识库
- `acme_it_support_kb` —— Acme 有限公司 IT支持知识库
- `acme_rd_product_kb` —— Acme 有限公司 研发产品知识库
- `acme_sales_marketing_kb` —— Acme 有限公司 销售市场知识库
- `acme_customer_success_kb` —— Acme 有限公司 客户成功知识库

#### Acme 有限公司 人力行政知识库（`acme_hr_admin_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 入职满一年后每年有多少天年假？ | `15 天` / `15天`<br><sub>跨企业对照锚点：Globex 是 20 天</sub> | `ACME-HR-001` |
| 2 | 当年没休完的年假可以顺延到什么时候？ | `3 月 31 日` / `3月31日` / `次年 3 月`<br><sub>跨企业对照锚点：Globex 是次年 6 月 30 日</sub> | `ACME-HR-001` |
| 3 | 每个月最多可以远程办公几天？ | `8 天` / `8天` | `ACME-HR-006` |
| 4 | 正式员工和试用期员工的离职通知期分别是多少天？ | `30 天` / `30天` | `ACME-HR-008` |
| 5 | 公司的核心工作时间是几点到几点？ | `10:00` / `16:00` | `ACME-HR-005` |

> 以上 5 条事实在本库的 `ACME-HR-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Acme 有限公司 财务知识库（`acme_finance_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 报销单笔超过多少金额需要 CFO 审批？ | `20000` / `20,000` / `2 万` / `2万` | `ACME-FIN-001` |
| 2 | 去一线城市出差，住宿费每晚上限是多少？ | `600` | `ACME-FIN-003` |
| 3 | 出差期间伙食补贴每天多少钱？ | `120` | `ACME-FIN-005` |
| 4 | 采购金额超过多少必须三家比价？ | `50000` / `50,000` / `5 万` / `5万` | `ACME-FIN-007` |
| 5 | 员工因公借款单次上限是多少？ | `20000` / `20,000` / `2 万` / `2万` | `ACME-FIN-011` |

> 以上 5 条事实在本库的 `ACME-FIN-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Acme 有限公司 IT支持知识库（`acme_it_support_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 域账号密码多久强制更换一次？ | `90 天` / `90天`<br><sub>跨企业对照锚点：Globex 的 WMS 是 60 天</sub> | `ACME-IT-001` |
| 2 | 密码连续输错几次会锁定账号，锁多久？ | `5 次` / `5次` / `30 分钟` / `30分钟` | `ACME-IT-001` |
| 3 | 堡垒机会话录像保留多少天？ | `180 天` / `180天` | `ACME-IT-004` |
| 4 | P1 级 IT 工单要求多久内响应？ | `30 分钟` / `30分钟` | `ACME-IT-005` |
| 5 | 收到钓鱼邮件应该转发到哪个邮箱？ | `security@acme.internal` | `ACME-IT-007` |

> 以上 5 条事实在本库的 `ACME-IT-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Acme 有限公司 研发产品知识库（`acme_rd_product_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 代码合并到主分支需要几名评审人通过？ | `2 名` / `2名` / `两名` | `ACME-RD-001` |
| 2 | 常规发布窗口是什么时候？ | `周四` / `20:00` | `ACME-RD-003` |
| 3 | P0 级线上故障要求多久内止血？ | `1 小时` / `1小时` | `ACME-RD-004` |
| 4 | 企业版客户的 API 限流阈值是多少？ | `5000` | `ACME-RD-008` |
| 5 | 弃用对外接口需要提前多久公告？ | `6 个月` / `6个月` | `ACME-RD-007` |

> 以上 5 条事实在本库的 `ACME-RD-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Acme 有限公司 销售市场知识库（`acme_sales_marketing_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 销售代表最高能给客户多少折扣？ | `10%`<br><sub>跨企业对照锚点：Globex 销售是 8%</sub> | `ACME-SM-001` |
| 2 | 专业版每个用户每月多少钱？ | `399` | `ACME-SM-002` |
| 3 | 官网线索需要多久内完成首次联系？ | `2 小时` / `2小时` | `ACME-SM-004` |
| 4 | 标准 POC 试用期是多久，可以延长几次？ | `14 天` / `14天` / `一次` | `ACME-SM-006` |
| 5 | 渠道代理商的标准返点是多少？ | `15%` | `ACME-SM-012` |

> 以上 5 条事实在本库的 `ACME-SM-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Acme 有限公司 客户成功知识库（`acme_customer_success_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 黄金套餐客户的工单多久内首次响应？ | `15 分钟` / `15分钟` | `ACME-CS-001` |
| 2 | 公司承诺的月度服务可用性是多少？ | `99.9` | `ACME-CS-002` |
| 3 | 发生重大故障后多久内要主动外呼客户？ | `30 分钟` / `30分钟` | `ACME-CS-004` |
| 4 | NPS 低于几分要标记为重点关注客户？ | `6 分` / `6分` | `ACME-CS-006` |
| 5 | 新客户的标准上手周期是多长？ | `30 天` / `30天` | `ACME-CS-009` |

> 以上 5 条事实在本库的 `ACME-CS-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### 🚫 alice_acme 应该被拒绝的问题（**比正向问题更重要**）

| # | 问题 | 类型 | 指向的库 | 为什么应该被拒 |
|---|---|---|---|---|
| 1 | 驾驶员连续驾驶最多多少小时就必须休息？ | 跨企业隔离 | `globex_hr_admin_kb` | 这是 Globex 的安全红线。alice 是 Acme 的企业管理员，本企业内是通配权限，但候选库先被 _org_owned_collections(Acme) 收敛过，Globex 的库根本不在候选集里 |
| 2 | 未保价的货物损坏了按运费的几倍赔付？ | 跨企业隔离 | `globex_customer_success_kb` | 同上，跨企业。Acme 是 SaaS 公司，本来也没有货物赔付这种业务 |
| 3 | 产品需求文档库里对需求评审有什么要求？ | 跨企业隔离 | `product_req_kb（测试新公司）` | 第三家企业的知识库，跨企业隔离 |

---
### bob_acme

| 项 | 值 |
|---|---|
| 用户名 | `bob_acme` |
| 密码 | `bob_acme123` |
| 所属企业 | Acme 有限公司 |
| 角色 | Acme人事部（企业角色） |
| 可访问知识库 | 1 个 |

> 受限员工：角色只关联了人力行政知识库，本企业其他 5 个库一律拒绝

可访问的知识库：

- `acme_hr_admin_kb` —— Acme 有限公司 人力行政知识库

#### Acme 有限公司 人力行政知识库（`acme_hr_admin_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 入职满一年后每年有多少天年假？ | `15 天` / `15天`<br><sub>跨企业对照锚点：Globex 是 20 天</sub> | `ACME-HR-001` |
| 2 | 当年没休完的年假可以顺延到什么时候？ | `3 月 31 日` / `3月31日` / `次年 3 月`<br><sub>跨企业对照锚点：Globex 是次年 6 月 30 日</sub> | `ACME-HR-001` |
| 3 | 每个月最多可以远程办公几天？ | `8 天` / `8天` | `ACME-HR-006` |
| 4 | 正式员工和试用期员工的离职通知期分别是多少天？ | `30 天` / `30天` | `ACME-HR-008` |
| 5 | 公司的核心工作时间是几点到几点？ | `10:00` / `16:00` | `ACME-HR-005` |

> 以上 5 条事实在本库的 `ACME-HR-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### 🚫 bob_acme 应该被拒绝的问题（**比正向问题更重要**）

| # | 问题 | 类型 | 指向的库 | 为什么应该被拒 |
|---|---|---|---|---|
| 1 | 报销单笔超过多少金额需要 CFO 审批？ | 本企业内无权限 | `acme_finance_kb` | **本企业内无权限**——bob 的角色「Acme人事部」只关联了人力行政知识库。这是最能体现角色级隔离的一条：同一家公司、同一份数据、换个账号就查不到 |
| 2 | 域账号密码多久强制更换一次？ | 本企业内无权限 | `acme_it_support_kb` | 同上，IT 支持知识库不在 bob 的角色权限内 |
| 3 | 驾驶员连续驾驶最多多少小时就必须休息？ | 跨企业隔离 | `globex_hr_admin_kb` | 跨企业。这条是 Globex 的安全红线，Acme 的人力行政库里没有任何对应内容——刻意挑一个「本企业没有近似话题」的问题，避免检索器拿 Acme 自己的年假文档来凑数 |

---
### carol_globex

| 项 | 值 |
|---|---|
| 用户名 | `carol_globex` |
| 密码 | `carol_globex123` |
| 所属企业 | Globex 环球集团 |
| 角色 | 企业管理员（org_admin）（系统角色） |
| 可访问知识库 | 6 个 |

> 另一家企业的管理员，本企业全库可见，但看不到 Acme 的任何内容

可访问的知识库：

- `globex_hr_admin_kb` —— Globex 环球集团 人力行政知识库
- `globex_finance_kb` —— Globex 环球集团 财务知识库
- `globex_it_support_kb` —— Globex 环球集团 IT支持知识库
- `globex_rd_product_kb` —— Globex 环球集团 研发产品知识库
- `globex_sales_marketing_kb` —— Globex 环球集团 销售市场知识库
- `globex_customer_success_kb` —— Globex 环球集团 客户成功知识库

#### Globex 环球集团 人力行政知识库（`globex_hr_admin_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 入职满一年后每年有多少天年假？ | `20 天` / `20天`<br><sub>跨企业对照锚点：Acme 是 15 天</sub> | `GLBX-HR-001` |
| 2 | 当年没休完的年假可以顺延到什么时候？ | `6 月 30 日` / `6月30日` / `次年 6 月`<br><sub>跨企业对照锚点：Acme 是次年 3 月 31 日</sub> | `GLBX-HR-001` |
| 3 | 驾驶员连续驾驶最多多少小时，必须休息多久？ | `4 小时` / `4小时` / `20 分钟` / `20分钟` | `GLBX-HR-002` |
| 4 | 仓储岗位夜班是几点到几点，夜班津贴多少？ | `22:00` / `80 元` / `80元` | `GLBX-HR-003` |
| 5 | 新员工安全生产培训需要多少学时？ | `16 学时` / `16学时` | `GLBX-HR-007` |

> 以上 5 条事实在本库的 `GLBX-HR-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Globex 环球集团 财务知识库（`globex_finance_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 客户对账单有异议须在几个工作日内提出？ | `7 个工作日` / `7个工作日` | `GLBX-FIN-001` |
| 2 | 报销单笔超过多少金额需要 CFO 审批？ | `30000` / `30,000` / `3 万` / `3万`<br><sub>跨企业对照锚点：Acme 是 20000 元</sub> | `GLBX-FIN-002` |
| 3 | 进口货物的关税预缴款要在到港前几天完成？ | `5 天` / `5天` | `GLBX-FIN-006` |
| 4 | 出口退税要在报关单放行后多少天内申报？ | `30 天` / `30天` | `GLBX-FIN-007` |
| 5 | 运营车辆的折旧年限是多少年？ | `8 年` / `8年` | `GLBX-FIN-012` |

> 以上 5 条事实在本库的 `GLBX-FIN-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Globex 环球集团 IT支持知识库（`globex_it_support_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | WMS 账号密码多久强制更换一次？ | `60 天` / `60天`<br><sub>跨企业对照锚点：Acme 域账号是 90 天</sub> | `GLBX-IT-001` |
| 2 | GPS 车载终端故障多久没恢复要联系设备厂商？ | `2 小时` / `2小时` | `GLBX-IT-003` |
| 3 | 系统常规变更窗口是什么时候？ | `周三` / `01:00` | `GLBX-IT-007` |
| 4 | WMS 库存对不上应该怎么排查？ | `盘点` / `出库` / `调拨` | `GLBX-IT-010` |
| 5 | 集团 IT 热线电话是多少？ | `400-820-9000` | `GLBX-IT-006` |

> 以上 5 条事实在本库的 `GLBX-IT-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Globex 环球集团 研发产品知识库（`globex_rd_product_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 路由优化算法要达到什么指标才允许灰度上线？ | `3%` | `GLBX-RD-001` |
| 2 | WMS 新功能全量推广前，库存差异率要控制在多少以内？ | `0.5%` | `GLBX-RD-002` |
| 3 | 平台的常规发布窗口是什么时候？ | `周三` / `01:00` | `GLBX-RD-003` |
| 4 | 运单和订单数据保留多久？ | `3 年` / `3年` | `GLBX-RD-008` |
| 5 | 客户要求删除个人信息，多久内要完成清理？ | `15 个工作日` / `15个工作日` | `GLBX-RD-008` |

> 以上 5 条事实在本库的 `GLBX-RD-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Globex 环球集团 销售市场知识库（`globex_sales_marketing_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 销售代表最高能给客户多少折扣？ | `8%`<br><sub>跨企业对照锚点：Acme 销售代表是 10%</sub> | `GLBX-SM-001` |
| 2 | 年运费承诺超过多少的大客户合同需要总部特批？ | `100 万` / `100万` | `GLBX-SM-001` |
| 3 | 运费报价单的有效期是多久？ | `15 天` / `15天` | `GLBX-SM-003` |
| 4 | 集团对外承诺的整体准点率是多少？ | `95%` | `GLBX-SM-005` |
| 5 | 保价服务怎么收费？ | `0.3%` / `30 元` / `30元` | `GLBX-SM-010` |

> 以上 5 条事实在本库的 `GLBX-SM-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### Globex 环球集团 客户成功知识库（`globex_customer_success_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 货物破损投诉要多久内首次响应？ | `4 小时` / `4小时` | `GLBX-CS-001` |
| 2 | 未保价的货物损坏了怎么赔付？ | `3 倍` / `3倍` / `2000` | `GLBX-CS-002` |
| 3 | 客户要改收货地址，多久内免费，超时收多少钱？ | `4 小时` / `4小时` / `80` | `GLBX-CS-004` |
| 4 | 货损理赔要在多少个工作日内完成？ | `15 个工作日` / `15个工作日` | `GLBX-CS-002` |
| 5 | 报关延误导致客户投诉，多久内要联系报关合规部？ | `2 小时` / `2小时` | `GLBX-CS-007` |

> 以上 5 条事实在本库的 `GLBX-CS-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### 🚫 carol_globex 应该被拒绝的问题（**比正向问题更重要**）

| # | 问题 | 类型 | 指向的库 | 为什么应该被拒 |
|---|---|---|---|---|
| 1 | 入职满一年后每年有 15 天年假吗？ | 跨企业隔离 | `acme_hr_admin_kb` | 15 天是 Acme 的口径。carol 是 Globex 管理员，应该只答出 Globex 的 20 天，或者明确说没有 15 天这个规定——**答出「是的，15 天」就说明串库了** |
| 2 | 堡垒机会话录像保留多少天？ | 跨企业隔离 | `acme_it_support_kb` | Acme 的 IT 制度，Globex 侧没有堡垒机这一说 |

---
### dave_globex

| 项 | 值 |
|---|---|
| 用户名 | `dave_globex` |
| 密码 | `dave_globex123` |
| 所属企业 | Globex 环球集团 |
| 角色 | 人力资源与行政知识库（企业角色） |
| 可访问知识库 | 1 个 |

> 受限员工：角色只关联了人力行政知识库，本企业其他 5 个库一律拒绝

可访问的知识库：

- `globex_hr_admin_kb` —— Globex 环球集团 人力行政知识库

#### Globex 环球集团 人力行政知识库（`globex_hr_admin_kb`）

| # | 问题 | 预期关键事实 | 数据出处 |
|---|---|---|---|
| 1 | 入职满一年后每年有多少天年假？ | `20 天` / `20天`<br><sub>跨企业对照锚点：Acme 是 15 天</sub> | `GLBX-HR-001` |
| 2 | 当年没休完的年假可以顺延到什么时候？ | `6 月 30 日` / `6月30日` / `次年 6 月`<br><sub>跨企业对照锚点：Acme 是次年 3 月 31 日</sub> | `GLBX-HR-001` |
| 3 | 驾驶员连续驾驶最多多少小时，必须休息多久？ | `4 小时` / `4小时` / `20 分钟` / `20分钟` | `GLBX-HR-002` |
| 4 | 仓储岗位夜班是几点到几点，夜班津贴多少？ | `22:00` / `80 元` / `80元` | `GLBX-HR-003` |
| 5 | 新员工安全生产培训需要多少学时？ | `16 学时` / `16学时` | `GLBX-HR-007` |

> 以上 5 条事实在本库的 `GLBX-HR-000 现行标准速查表（统一口径）` 里也有汇总，演示时如果模型引用了旧的部门／枢纽特例，请翻这一篇核对。

#### 🚫 dave_globex 应该被拒绝的问题（**比正向问题更重要**）

| # | 问题 | 类型 | 指向的库 | 为什么应该被拒 |
|---|---|---|---|---|
| 1 | 客户对账单有异议须在几个工作日内提出？ | 本企业内无权限 | `globex_finance_kb` | **本企业内无权限**——dave 的角色只关联了人力行政知识库 |
| 2 | 货物破损投诉要多久内首次响应？ | 本企业内无权限 | `globex_customer_success_kb` | 同上，客户成功知识库不在 dave 的角色权限内 |
| 3 | Acme 的员工每月最多可以远程办公几天？ | 跨企业隔离 | `acme_hr_admin_kb` | 跨企业。dave 同样有「人力行政」类目权限，但那是 **Globex 的** |

---
## 3. 跨账号对照表（演示隔离最快的方式）

同一个问题，四个账号分别问，看结果是不是按预期分叉。

### 「入职满一年后每年有多少天年假？」

| 账号 | 企业 | 预期结果 |
|---|---|---|
| `alice_acme` | Acme 有限公司 | 15 天（Acme 口径） |
| `bob_acme` | Acme 有限公司 | 15 天（Acme 口径） |
| `carol_globex` | Globex 环球集团 | 20 天（Globex 口径） |
| `dave_globex` | Globex 环球集团 | 20 天（Globex 口径） |

> 四个账号都有各自企业人力行政库的权限，**同一个问题必须答出两个不同的数字**。如果四个账号答出同一个数字，或者某个账号同时提到 15 和 20，就是跨企业串库。

### 「当年没休完的年假可以顺延到什么时候？」

| 账号 | 企业 | 预期结果 |
|---|---|---|
| `alice_acme` | Acme 有限公司 | 次年 3 月 31 日 |
| `bob_acme` | Acme 有限公司 | 次年 3 月 31 日 |
| `carol_globex` | Globex 环球集团 | 次年 6 月 30 日 |
| `dave_globex` | Globex 环球集团 | 次年 6 月 30 日 |

> 同上，第二个独立锚点。两条同时对得上，基本可以排除「碰巧蒙对」。

### 「报销单笔超过多少金额需要 CFO 审批？」

| 账号 | 企业 | 预期结果 |
|---|---|---|
| `alice_acme` | Acme 有限公司 | 20000 元（Acme 口径） |
| `bob_acme` | Acme 有限公司 | 🚫 无权访问（角色里没有财务库） |
| `carol_globex` | Globex 环球集团 | 30000 元（Globex 口径） |
| `dave_globex` | Globex 环球集团 | 🚫 无权访问（角色里没有财务库） |

> 这一行同时演示两件事：**跨企业数据不同** + **同企业内按角色拒绝**。两个 org_admin 答出不同数字，两个受限员工被拒。

### 「销售代表最高能给客户多少折扣？」

| 账号 | 企业 | 预期结果 |
|---|---|---|
| `alice_acme` | Acme 有限公司 | 10%（Acme 口径） |
| `bob_acme` | Acme 有限公司 | 🚫 无权访问（角色里没有销售市场库） |
| `carol_globex` | Globex 环球集团 | 8%（Globex 口径） |
| `dave_globex` | Globex 环球集团 | 🚫 无权访问（角色里没有销售市场库） |

> 同上结构，换一个部门再验一次，避免只在财务库上碰巧成立。

### 「密码多久强制更换一次？」

| 账号 | 企业 | 预期结果 |
|---|---|---|
| `alice_acme` | Acme 有限公司 | 域账号 90 天（Acme 口径） |
| `bob_acme` | Acme 有限公司 | 🚫 无权访问（角色里没有 IT 支持库） |
| `carol_globex` | Globex 环球集团 | WMS 账号 60 天（Globex 口径） |
| `dave_globex` | Globex 环球集团 | 🚫 无权访问（角色里没有 IT 支持库） |

> 两家企业连「密码策略」这种最容易雷同的制度都给了不同数字，串库时最容易在这一行暴露。
---

## 4. 数据是怎么造出来的 / 怎么复现

```bash
set -a; source .env; set +a
export RAGENT_DEBUG=true

# 看一遍会做什么，不落盘
.venv/bin/python scripts/generate_demo_kb_dataset.py --dry-run

# 生成语料 + 真实摄入 + 重新生成本文档
.venv/bin/python scripts/generate_demo_kb_dataset.py

# 用真实账号 + 真实 ACL 抽查
.venv/bin/python scripts/generate_demo_kb_dataset.py --stage verify
```

- 语料源码：`scripts/demo_kb_content/`（每个库一个模块），问题库：`scripts/demo_kb_content/questions.py`
- 语料文件：`data/demo_kb_corpus/{tenant}/{category}/`，清单 `data/demo_kb_corpus/manifest.json`
- 摄入走真实 `IngestionPipeline`，不是手工拼 BM25/Chroma 索引
- 脚本幂等：语料内容确定、摄入按文件 SHA256 跳过已有文件，可以反复跑
- ⚠️ `data/` 整个目录在 `.gitignore` 里，**语料文件不进版本库**——
  换一台机器要先跑一遍 `--stage generate`。语料的事实来源是
  `scripts/demo_kb_content/`（已入库），不是那些 txt 文件

🔴 脚本里有硬保护：只允许写 `acme_*_kb` / `globex_*_kb` 这 12 个库。
`product_req_kb`（端到端黄金测试 15/17 基线）和 `mmarco`（recall@10 85.0% 基线）
是评估语料，往里加数据会让全部质量基线失效。

---

## 5. 交付三句话（CLAUDE.md §7.3）

**验收怎么做**
用 §2 的四个账号分别登录（密码见各账号表格），照着「该账号可访问知识库」小节里的问题提问，
对着「预期关键事实」判断对错；再问 🚫 小节的问题，确认拿到「无权访问」或查不到对方企业的内容。
最快的一条路径是 §3 的跨账号对照表：同一个问题四个账号问一遍，看结果是不是按预期分叉。
⚠️ 提问时**点名知识库**（「在 IT 支持知识库里……」），原因见 §0.2。

**回归怎么保**
`scripts/generate_demo_kb_dataset.py --stage verify` 会用真实账号走真实 ACL 把
全部 60 条正向问题 + 11 条拒绝用例跑一遍，结果落 `scripts/demo_dataset_results/verify_*.json`
（含 commit hash 与脏文件清单），`--stage doc` 再把数字渲染进 §0.3。
语料本身是确定性生成的，`--stage generate` 重跑逐字节一致，摄入按 SHA256 幂等跳过。

**什么没做**
见下面 §6，逐条列了未覆盖的范围。最需要先知道的两条：
`top_docs=5` 粗筛瓶颈只定位未修复（§0.2）；原有 20 篇随机语料与统一口径的矛盾只做了口径覆盖、没有删除（§0.1）。

---

## 6. 本次未覆盖的范围

- **没有改任何权限配置**。四个账号的角色与知识库关联全部沿用现网既有状态，
  本次一行 DB 都没动。因此 `bob_acme` 与 `dave_globex` 的权限形状相同（各自企业的人力行政库），
  这不是遗漏，是刻意保持——改动它们会影响 `tests/integration/test_department_kb_parallel_recall.py`
  里已有的断言。
- **`__summary` 层同步摄入，但没有单独验证层次化检索的效果**。
- **没有测多轮对话、没有测工作流与考勤工具**，只覆盖知识库问答这一条链路。
- **正向问题的判据是「回答里出现预期关键字」**，不是语义正确性判定——
  模型把数字答对但语境说反的情况，这份判据抓不到，需要人读回答原文。
- **没有做并发验证**，全部是串行单用户抽查。
- **原有 20 篇随机语料的矛盾没有根除**，只是用 `-000` 速查表做了口径覆盖（见 §0 的警告）。
  彻底解决要先修 `BM25Indexer.remove_document` 这个 P0，不在本次范围内。
- **`top_docs=5` 这个粗筛瓶颈只定位、未修复**（见 §0.2）。因此本文里相当一部分
  正向问题，**在当前默认配置下、不点名知识库直接问，会返回「未找到相关结果」**
  ——这不是数据缺失，是粗筛层没选中。单库账号受影响更小但同样会漏。
  具体通过率见 §0.3 的实测表。
- **`acme_it_support_kb` 里有排查留下的多余文档**：定位 §0.2 那个问题时，
  先后试过两个错误假设，各留下了一批对照文档在库里——
  ① 按「正文在前、抬头在后」的版式重新摄入过一遍全库（101 篇，内容与现版本完全一致）；
  ② 两篇单句探针文档（`PROBE-IT-A_域账号密码更换周期`、`PROBE-IT-B_钓鱼邮件上报邮箱`），
  用来验证「文档太长」这个假设，结论是也不成立。
  这些没有强行清理：平台的按文档删除链路本身就是未闭环的 P0（CLAUDE.md §4 第 7 条），
  硬删要直接动 Chroma / BM25 / OpenSearch 三处索引，风险大于收益。
  它们**不会产生矛盾答案**（内容与正式版本一致），只是让该库文档数偏多。
  真要清理，建议跟那个 P0 一起处理。
- **拒绝类问题只验证了「拿不到内容」**，没有逐条核对审计日志是否记录了拒绝事件。
